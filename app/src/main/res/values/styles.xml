<resources>


    <style name="Base.Theme.DoacaoMais" parent="Theme.AppCompat.Light.DarkActionBar" />

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.DoacaoMais">

        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorHighlight</item>


        <item name="android:textColorPrimary">@color/colorPreto</item>
        <item name="android:textColorSecondary">@color/color_grey</item>
        <item name="android:textColor">@color/colorHighlight</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/colorPreto</item>
        <item name="android:textColorSecondary">@color/color_grey</item>
        <item name="android:textColor">@color/colorPreto</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/colorPreto</item>
        <item name="android:textColorSecondary">@color/color_grey</item>
        <item name="android:textColor">@color/colorPreto</item>
    </style>


    <style name="CollapsedAppBar" parent="@android:style/TextAppearance.Small">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorPreto</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
